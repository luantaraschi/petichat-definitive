{
  "name": "petichat",
  "version": "0.1.0",
  "private": true,
  "description": "PetiChat - Plataforma de IA Jurídica para Advogados Brasileiros",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "db:generate": "pnpm --filter @petichat/api db:generate",
    "db:migrate": "pnpm --filter @petichat/api db:migrate",
    "db:push": "pnpm --filter @petichat/api db:push",
    "db:studio": "pnpm --filter @petichat/api db:studio"
  },
  "keywords": ["legal", "ai", "saas", "brazil", "lawyers"],
  "author": "",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
